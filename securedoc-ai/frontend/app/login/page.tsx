'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {ShieldCheck} from 'lucide-react';import {api} from '../../lib/api';
export default function Login(){const [email,setEmail]=useState('demo@securedoc.local'),[password,setPassword]=useState('Demo@123456'),[error,setError]=useState(''),[busy,setBusy]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();setBusy(true);setError('');try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('securedoc_token',d.token);r.push('/dashboard')}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="min-h-screen bg-slate-50 grid place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-sm"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-600 text-white"><ShieldCheck size={20}/></div><div><b>SecureDoc AI</b><p className="text-xs text-slate-400">Private document intelligence</p></div></div><h1 className="mt-8 text-2xl font-bold">Welcome back</h1><p className="mt-1 text-sm text-slate-500">Sign in to your workspace.</p><label className="mt-6 block text-sm font-medium">Email<input className="mt-2 w-full rounded-xl border p-3" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-4 block text-sm font-medium">Password<input type="password" className="mt-2 w-full rounded-xl border p-3" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={busy} className="mt-6 w-full rounded-xl bg-indigo-600 p-3 font-semibold text-white disabled:opacity-60">{busy?'Signing in...':'Sign in'}</button><p className="mt-5 text-center text-sm text-slate-500">New here? <Link className="font-semibold text-indigo-600" href="/register">Create an account</Link></p><div className="mt-5 rounded-xl bg-slate-50 p-3 text-xs text-slate-500">Demo login: demo@securedoc.local / Demo@123456</div></form></main>}
