'use client';import {useEffect,useState} from 'react';import {Shell} from '../../components/shell';import {Card} from '../../components/ui';import {api} from '../../lib/api';import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,AreaChart,Area} from 'recharts';
export default function Analytics(){const [o,setO]=useState<any>(),[s,setS]=useState<any>();useEffect(()=>{Promise.all([api('/analytics/overview'),api('/analytics/spending')]).then(([a,b])=>{setO(a);setS(b)})},[]);if(!o||!s)return <Shell><Card className="p-8">Loading analytics...</Card></Shell>;return <Shell><h1 className="text-2xl font-bold">Analytics</h1><p className="mt-1 text-sm text-slate-500">Live analytics from your stored invoices.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Total spending',`${o.total_spending.toLocaleString()} BDT`],['Invoices',o.total_invoices],['Vendors',o.vendors],['Pending review',o.pending_review]].map(x=><Card key={x[0]} className="p-5"><p className="text-sm text-slate-500">{x[0]}</p><p className="mt-2 text-2xl font-bold">{x[1]}</p></Card>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-2"><Card className="p-5"><h2 className="font-bold">Monthly spending</h2><div className="mt-5 h-72"><ResponsiveContainer><AreaChart data={s.months}><XAxis dataKey="month"/><YAxis/><Tooltip/><Area type="monotone" dataKey="value" stroke="#4f46e5" fill="#eef2ff"/></AreaChart></ResponsiveContainer></div></Card><Card className="p-5"><h2 className="font-bold">Top vendors</h2><div className="mt-5 h-72"><ResponsiveContainer><BarChart data={s.vendors} layout="vertical"><XAxis type="number"/><YAxis type="category" dataKey="name" width={100}/><Tooltip/><Bar dataKey="value" fill="#6366f1" radius={[0,6,6,0]}/></BarChart></ResponsiveContainer></div></Card></div></Shell>}
