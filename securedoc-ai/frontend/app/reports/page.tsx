'use client';import {Shell} from '../../components/shell';import {Card} from '../../components/ui';import {downloadBlob} from '../../lib/api';

export default function Reports(){return <Shell><h1 className="text-2xl font-bold">Reports & exports</h1><p className="mt-1 text-sm text-slate-500">Export the live invoice data from your workspace.</p><div className="mt-6 grid gap-4 md:grid-cols-2"><Card className="p-6"><h2 className="font-bold">Invoice dataset</h2><p className="mt-2 text-sm text-slate-500">Download all invoices with financial fields, status and confidence.</p><div className="mt-5 flex gap-2"><button onClick={()=>downloadBlob('/reports/export.csv','securedoc-invoices.csv')} className="rounded-xl border px-4 py-2 text-sm font-semibold">CSV</button><button onClick={()=>downloadBlob('/reports/export.xlsx','securedoc-invoices.xlsx')} className="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Excel</button></div></Card><Card className="p-6"><h2 className="font-bold">PDF reports</h2><p className="mt-2 text-sm text-slate-500">Generate a shareable invoice report for your current workspace.</p><button onClick={()=>downloadBlob('/reports/export.pdf','securedoc-invoice-report.pdf')} className="mt-5 rounded-xl border px-4 py-2 text-sm font-semibold">PDF</button></Card></div></Shell>}
