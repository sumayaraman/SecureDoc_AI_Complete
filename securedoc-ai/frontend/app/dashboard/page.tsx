'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {Shell} from '../../components/shell';import {Card,Badge} from '../../components/ui';import {Files,WalletCards,Users,AlertTriangle,ArrowUpRight} from 'lucide-react';import {api} from '../../lib/api';
export default function Dashboard(){const [d,setD]=useState<any>(null),[s,setS]=useState<any>(null);useEffect(()=>{Promise.all([api('/analytics/overview'),api('/analytics/spending')]).then(([a,b])=>{setD(a);setS(b)})},[]);if(!d)return <Shell><div className="py-20 text-center text-sm text-slate-500">Loading dashboard...</div></Shell>;const cards=[[Files,'Total Documents',d.total_documents],[WalletCards,'Total Spending',`৳${Number(d.total_spending).toLocaleString()}`],[Users,'Vendors',d.vendors],[AlertTriangle,'Pending Review',d.pending_review]];return <Shell><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-indigo-600">Workspace overview</p><h1 className="mt-1 text-2xl font-bold">Business dashboard</h1><p className="mt-1 text-sm text-slate-500">A live view of your organization's document data.</p></div><Badge tone="green">● Live data</Badge></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([I,l,n]:any)=><Card className="p-5" key={l}><div className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-50 text-indigo-600"><I size={19}/></div><div className="mt-5 text-2xl font-bold">{n}</div><div className="mt-1 flex items-center justify-between text-xs"><span className="text-slate-500">{l}</span><ArrowUpRight size={14} className="text-slate-300"/></div></Card>)}</div><div className="mt-6 grid gap-5 xl:grid-cols-2"><Card className="p-5"><h2 className="font-bold">Top vendors</h2><div className="mt-4 space-y-3">{(s?.by_vendor||[]).slice(0,7).map((x:any)=><div key={x.vendor} className="flex items-center justify-between rounded-xl bg-slate-50 p-3 text-sm"><span>{x.vendor}</span><b>৳{Number(x.amount).toLocaleString()}</b></div>)}{!s?.by_vendor?.length&&<p className="text-sm text-slate-500">Upload invoices to see vendor analytics.</p>}</div></Card><Card className="p-5"><div className="flex items-center justify-between"><div><h2 className="font-bold">Quick actions</h2><p className="text-xs text-slate-500">Continue working with your documents.</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><Link href="/upload" className="rounded-xl bg-indigo-600 p-4 text-sm font-semibold text-white">Upload invoices</Link><Link href="/invoices" className="rounded-xl border p-4 text-sm font-semibold">Review invoices</Link><Link href="/analytics" className="rounded-xl border p-4 text-sm font-semibold">View analytics</Link><Link href="/assistant" className="rounded-xl border p-4 text-sm font-semibold">Ask AI assistant</Link></div></Card></div></Shell>}
