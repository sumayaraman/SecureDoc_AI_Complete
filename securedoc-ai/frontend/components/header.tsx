'use client';
import Link from 'next/link';import {Search,Bell,Plus,Menu} from 'lucide-react';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {Button} from './ui';import {api} from '../lib/api';
export function Header(){const [notes,setNotes]=useState<any[]>([]),[open,setOpen]=useState(false),r=useRouter();useEffect(()=>{api('/notifications').then(d=>setNotes(d.items||[])).catch(()=>{})},[]);return <header className="sticky top-0 z-20 flex h-16 items-center justify-between border-b border-slate-200 bg-white/95 px-4 backdrop-blur sm:px-6"><Link href="/dashboard" className="font-bold lg:hidden">SecureDoc AI</Link><div className="relative hidden w-full max-w-md md:block"><Search className="absolute left-3 top-2.5 text-slate-400" size={18}/><input placeholder="Search invoices, vendors, documents..." className="w-full rounded-xl bg-slate-50 py-2.5 pl-10 pr-4 text-sm outline-none ring-1 ring-transparent focus:ring-indigo-200"/></div><div className="ml-auto flex items-center gap-2"><div className="relative"><button onClick={()=>setOpen(!open)} className="relative rounded-xl p-2.5 text-slate-500 hover:bg-slate-50"><Bell size={19}/>{notes.some(n=>!n.read)&&<span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-indigo-600"/>}</button>{open&&<div className="absolute right-0 mt-2 w-80 rounded-2xl border bg-white p-3 shadow-xl">{notes.length?notes.slice(0,5).map(n=><div key={n.id} className="border-b p-3 last:border-0"><b className="text-sm">{n.title}</b><p className="text-xs text-slate-500">{n.message}</p></div>):<p className="p-4 text-sm text-slate-500">No notifications.</p>}</div>}</div><Button className="hidden sm:flex" onClick={()=>r.push('/upload')}><Plus size={16} className="mr-1.5"/>Upload</Button><button className="rounded-xl p-2.5 lg:hidden" onClick={()=>r.push('/documents')}><Menu size={19}/></button></div></header>}
