# API

- GET /health
- GET /api/documents
- GET /api/analytics/overview
- POST /api/assistant/query

The API is intentionally small in Phase 1. Invoice, upload, reports, auth, and audit endpoints should be added behind the same organization authorization boundary in later phases.
